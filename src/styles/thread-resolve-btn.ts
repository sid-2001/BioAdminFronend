import { Button } from "@mui/material";
import { styled } from "@mui/material/styles";

export const ResolveBtn = styled(Button)`
  padding: 8px 16px;
  border-radius: 8px;
  background: rgba(212, 227, 255, 0.5);
  color: var(--Primary, #305dcf);
  font-size: 14px;
  font-weight: 600;
  line-height: normal;
  cursor: pointer;
  border: none;
  outline: none;
`;
